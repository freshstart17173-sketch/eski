node_modules/
package-lock.json
package.json

# generated by the smoke suite, never committed
tests/fixtures/
tests/vendor/
tests/dl/

# owner-swappable, intentionally absent from the repo
demo.eski

# media stays out of git: host it on R2 and point library/index.json at it
*.eski
!tests/**/*.eski
