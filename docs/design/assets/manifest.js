/* Asset manifest — the list of files actually present in this folder.
   The gallery loads ONLY what's listed here, so an empty list means zero
   missing-file requests (deterministic, no console noise) and the gradient
   fallbacks stay. See README.md.

   To populate: drop your files into this folder, then add their names below
   (or regenerate — the one-liner is in README.md). */
window.__eskiAssets = [
  // "cover_bloom.png",
  // "ref_drums.png",
  // "sh040_comp.mp4",
];
