================================================================================
ESKI — PROJECT CATCHUP  (read this first in a new chat)
================================================================================
Purpose of this file: bring a fresh session fully up to speed on what eski is,
how every file works, the hard rules, how to test, and the pending work.
Keep it updated as the project changes. Companion docs: TODO.txt (backlog),
ERRORS.txt (the ESK-#### failure codes), ICONS.txt (icon inventory),
spec.html (the human-readable format spec).

--------------------------------------------------------------------------------
1. WHAT ESKI IS
--------------------------------------------------------------------------------
eski is a file format + web app for comics with a soundtrack. One ".eski" file
(a plain zip) holds the page images, the audio, and the manifest that maps audio
to pages. Turn a page and the music follows. Also supports user-triggered
"one-shots" (per-page sounds for dubbing/SFX).

Three apps, all vanilla JS, NO build step, NO frameworks. Only runtime dep is
JSZip 3.10.1 from cdnjs. pdf.js 3.11.174 is lazy-loaded from cdnjs for PDF import.
- index.html    = the LIBRARY, and the HOME PAGE (grid of eskis from library/)
- read.html     = the READER (open + read an .eski)
- studio.html   = the STUDIO (author/edit an .eski)
- spec.html     = the format spec (mostly text; hidden from nav, linked in footer)

--------------------------------------------------------------------------------
2. REPO LAYOUT
--------------------------------------------------------------------------------
index.html        HOME (renamed from "library" in the ui on 2026-07-28; the
                  library/ FOLDER keeps its name). the shelf, plus a search
                  that stays collapsed to its icon until clicked, matching on
                  title, author and series. "/" focuses it.
read.html         reader (single file: HTML+CSS+JS inline)
studio.html       studio: the authoring app (single file)
spec.html         format spec (single file) — SOURCE OF THE GNOMON @font-face
sw.js             service worker (offline shell; network-first HTML)
manifest.json     PWA web app manifest (+ file_handlers for .eski)
eski_logo.png     800x800 logo (favicon + PWA icon)
library/          drop .eski files here; index.html lists them (README.txt inside)
demo.eski         NOT in repo — owner drops it at root; reader fetches by filename
tests/            Playwright smoke suite (smoke.js, make-fixtures.js, vendor-pdfjs.js, README.md)
ICONS.txt         inventory of every Lucide icon used, per page
TODO.txt          backlog / done log / deployment notes
catchup.txt       this file

--------------------------------------------------------------------------------
3. HARD RULES / CONVENTIONS / GOTCHAS  (violate these and things break)
--------------------------------------------------------------------------------
* Vanilla JS only, no bundler, no framework. New CDN libs only when a feature
  demands it (JSZip, pdf.js). The "one self-contained HTML file per app" rule
  now has TWO deliberate exceptions, both shared: platform.js (auth, 14.8) and
  tokens.css (the design system, D18). Component CSS still lives per page.
* GNOMON FONT + DESIGN TOKENS: both live in tokens.css at the repo root, which
  all four pages link. The old "/*FONTFACE*/ placeholder + node one-liner to
  re-inject from spec.html" ritual is GONE, along with the 5.5 KB of base64
  that used to be duplicated in all four files. To restyle anything, edit
  tokens.css first and only add a page rule if the token cannot express it.
  (node is at "C:\Program Files\nodejs\node.exe" on this machine; the `node`
  alias may not be on PATH in the Bash tool — use the full path or add to PATH.)
* ICONS: each page inlines a small `const ICONS = {name: "<svg inner>"}` map
  (Lucide v1.25.0, ISC license) + a paintIcons() that fills every [data-ico]
  element with an <svg class="ico">. Buttons keep their title text. To add an
  icon, curl https://unpkg.com/lucide-static@1.25.0/icons/<name>.svg and paste
  the inner markup into the map.
* SERVICE WORKER STALENESS: sw.js is now network-first for HTML (deploys land on
  a normal reload) and cache-first for assets (CACHE='eski-v4'). BUT an already-
  installed OLD worker keeps controlling the page until its next navigation, so
  during local dev after editing you often must unregister the SW or reload twice.
  In the smoke tests we set serviceWorkers:'block' to avoid this entirely.
* demo.eski is fetched by filename and is owner-swappable; NEVER hardcode its
  contents. It is intentionally absent from the repo. The reader also accepts
  ?read=<relative-path> to open a specific file (used by the library links).
* DISCORD invite is https://discord.gg/UFNpnHAbD (DISCORD const in index.html,
  static hrefs in read.html header/footer). composer/spec have no discord link.
* BUTTONS DO NOT INHERIT THE PAGE FONT. Every page needs
  button,input,select,textarea{font-family:inherit;font-size:inherit} or
  button-based UI (the library's .cover cards) silently renders in Arial.
* READER-SIDE OVERRIDES (reading mode, playback mode, direction, crossfade,
  one-shot loop) change what the reader DOES but must NEVER mutate the manifest.
  The composer writes exactly what the author authored. Smoke tests assert this.
* POSITION MEMORY is keyed by comic TITLE (localStorage 'eski-pos-<title>').
  Known weakness: two comics with the same title share a saved page. (backlog fix)
* VERCEL has no directory autoindex, so library.html's "just drop files in
  library/" auto-listing does NOT work there — on Vercel you must ship a
  library/index.json. Locally / on autoindex hosts it works with zero config.
* DON'T commit .eski files to git (repo bloat + Vercel bandwidth). Host media on
  Cloudflare R2 (free egress) and point library/index.json at R2 URLs.
* JS SCOPE GOTCHA when testing: top-level `let`/`const` (cState, os, zoom, audio,
  oneshotsFor...) ARE reachable by bare name inside Playwright page.evaluate()
  (they live in the global lexical env). They are NOT reachable via `window.` for
  let/const; only `function foo(){}` declarations attach to window. The in-app
  browser's javascript_tool eval canNOT see bare let/const at all — use
  window.<fn> there and it only sees function declarations.
* Copy style: lowercase headlines/labels, terse, plainspoken. Say "an .eski",
  never "a .eski". NO em dashes anywhere. Keep the reader minimal.

--------------------------------------------------------------------------------
4. FILE FORMAT (manifest v2)  — full spec in spec.html
--------------------------------------------------------------------------------
An .eski is a plain zip:
  001.png ... 047.png        pages, sorted by filename (numeric-aware)
  audio/                     the audio files
  .eski/cover.png            optional cover copy (reader samples it for the tint)
  .eski/manifest.json        the map

manifest.json (version 2):
{
  "version": 2, "created": "...ISO...", "app": "eski",
  "meta": { "title": "", "creator": null, "description": null,
            "direction": "ltr" | "rtl", "cover": "001.png" },
  "player": { "volume": 80, "crossfade": 1.0, "loopMode": "loop" | "next",
              "playbackMode": "sync" | "playlist", "readingMode": "pages" | "scroll",
              "oneshotLoop": false },
  "tracks": [
    { "id":"t1","title":"","source":"local","file":"audio/01-x.mp3",
      "type":"music","volume":100,"sync":{"from":3,"start":0} },
    { "id":"t2","title":"quiet","source":"local","file":null,
      "type":"silence","volume":100,"sync":{"from":5,"start":0} },
    { "id":"o1","title":"line one","source":"local","file":"audio/03-o1.mp3",
      "type":"oneshot","volume":100,"sync":{"from":4,"start":0} }
  ],
  "pages": { "count": 7, "naming": "auto" }
}

Rules:
- TYPES: "music" and "silence" own PAGE RANGES; "oneshot" does NOT (reader triggers it).
- OWNERSHIP: sort music/silence by sync.from; a track owns pages from its trigger
  to the page before the next trigger (last owns to the end). Pages before the
  first trigger are silent. One-shots are excluded from this computation.
- QUEUES: music tracks sharing a sync.from form a queue in manifest order; when
  one ends the next continues within the same range; after the last, loopMode
  decides restart vs quiet.
- ONE-SHOTS sharing a sync.from form an ordered list (manifest order) the reader
  walks with a cursor. sync.start = in-point offset. sync.end (optional, music)
  = out-point. player.oneshotLoop = default for cursor wrap.
- v1 files with streaming tracks ("source":"yt"/"sc") load but those tracks are
  unavailable/silent and dropped on re-export. Plain cbz/zip open as silent comics.

--------------------------------------------------------------------------------
5. READER  (read.html) — how it works
--------------------------------------------------------------------------------
Boot: sets theme, paintIcons, reads ?read= (else demo.eski), fetch -> JSZip ->
comicFromZip -> openComic. body gets .needs-gesture until first user interaction
(browsers hold audio).

LAZY LOADING: pages keep their JSZip entry and decompress on demand
(ensurePageUrl) with prefetchAround(+/-3). Scroll mode uses an IntersectionObserver
to load pages as they approach the viewport.

AUDIO ENGINE (const audio): dual <audio> elements a/b for equal-power-ish
crossfades (fade()). No global play... EXCEPT space now pauses/resumes the
soundtrack (owner override of the old "no pause" rule). audio.resolve(track)
lazy-decompresses audio. loadSeq guards against stale async loads on fast turns.
- checkPageTrack(): on page change picks the queue for audioPage() and plays
  its first track (or continues if mid-queue). onTrackEnd() advances the queue /
  loops / goes quiet per loopMode. Silence tracks fade the previous out.
- audioPage(): in spread mode the LEFTMOST visible page decides the audio.
- Playlist mode (effPlayback()==='playlist'): tracks advance on 'ended',
  independent of page turns; page bar still shows ranges.

READING MODES: effReadingMode() = 'pages' | 'scroll' (file default + reader
override). Pages mode = one page (or two in spread). Scroll mode = vertical
scroller; current page = the one crossing the viewport midline.

PAGE ZOOM/PAN (pages mode only): wheel zooms toward cursor (1x-5x), drag pans
when zoomed, double-click toggles, turning the page resets. Click-zones disable
while zoomed. State: zoom/panX/panY, applyZoom(), zoomAt(), resetZoom().

DIRECTION: isRTL() flips arrows, click zones, spread pairing, page bar direction.
TINT ("colour match"): applyTint() samples the cover hue -> --hue/--tintsat CSS
vars; toggleable; off = neutral grey. Dark theme derives from the same vars.

FULLSCREEN FOCUS (toggleFocus): real requestFullscreen on documentElement, hides
all chrome; mousemove "peeks" the player bar; settings popover stays usable.

DEEP LINKS: #comic-title/page=N applied on load; hash updated via replaceState
on nav. slugify(title) keys it.

ONE-SHOTS (const os): os.list = one-shots for the current page (reset on page
turn, monophonic os.el channel). Keys: DOWN = next, UP = previous (no wrap unless
loop). shift+space = pause/resume the current one-shot. Corner indicator (#os-hint)
shows only when the page has one-shots: speaker + "n/total" + up/down taps, and a
.playing class (pulsing speaker + #os-prog progress bar) while audio plays.
effOsLoop() from settings.osloop (seeded by player.oneshotLoop). First-run
tutorial popup (#tut-os). Hotkeys panel (#keys-panel) toggled by "?" or #keys-btn.

CONTROLS: player bar (bottom): now-playing, page bar (colored per track) + song
progress bar, page jump, spread/focus/new-file/hotkeys/settings/mute/volume.
The old < > nav arrow BUTTONS were removed; page turns via click zones + arrows.
Settings popover (#settings): colour match, reading, playback, direction,
crossfade (file/fixed/off), one-shots loop. Dark-mode toggle is currently a
header button (contrast icon) — see PENDING #0.

HOTKEYS: arrows/click = pages; space = soundtrack pause; down/up = one-shots;
shift+space = one-shot pause; s spread; f focus; m mute; d dark; ? hotkeys; esc.

--------------------------------------------------------------------------------
6. STUDIO  (studio.html) — how it works
--------------------------------------------------------------------------------
STATE: cState = { pages:[{name,blob,url,thumb}], tracks:[{tid,aud,type,title,
from,start,volume}], meta:{title,creator,description,direction}, player:{...},
coverIndex }. audioStore[aud] = {blob,url,fileName,peaks,duration}. audioPool =
ordered aud ids for the bay. osBlocks = Set of pages with an (even empty) one-shot
block. undoStack/redoStack snapshot (JSON) of tracks/meta/player/coverIndex.

LAYOUT: layered surfaces --bg-0..--bg-3 + shadows (NOT the reader palette; the
composer commits to depth over brand purity, by owner request). Header: add media,
add folder, open .eski, settings, export, dark. Left = MEDIA BAY. Center = STAGE
(preview on top, timeline below). Right = settings DRAWER (toggle).

MEDIA BAY: images (page thumbnails) + audio blocks (with mini waveforms). Search
box + filter (all/images/audio/unused) + audio sort (added/name/duration). Zoom
+/- for image chips. Blocks are draggable (dataTransfer text/eski-img, /-aud).
Import: "add media" (images/cbz/pdf/audio), "add folder" (webkitdirectory input +
recursive folder DROP via readEntryTree), "open .eski" (round-trip edit).

TIMELINE has TWO modes (modeswitch): 'sound' and 'pages'.
- SOUNDTRACK MODE: a ruler + a page-range row (one card per trigger page, with a
  thumbnail) + track lanes. Bands: drag the top GRIP to move the trigger page
  (snaps); drag the BODY to SLIP the in-point (waveform translates, pxPerSec
  scales with zoom); 2s scrub preview on release. Queues stack in lanes. Minimap
  zoom/pan bar with handles; "fit" zooms to the whole comic; ctrl+scroll zoom,
  shift+scroll pan, ctrl+shift+scroll = lane height. Selecting a band shows a
  compact CLIPBAR (name, volume, preview 2s, +queue, remove) in the toolbar.
  "add silence" button (sound mode only). High-res waveforms (~120 buckets/sec).
- PAGES MODE: every page as a thumbnail card (reorder by drag, remove via x,
  drop images to add). Page zoom +/-. Each page can hold ONE one-shot BLOCK
  (badge on the card with count); a draggable "one-shot" TOOL in the toolbar
  drops onto a page to create the block; clicking the badge opens the QUEUE PANEL.

ONE-SHOTS (composer): tracks with type 'oneshot'. oneshotsFor(page) = ordered
list. QUEUE PANEL (#qp, currently a CENTER MODAL): lists one-shots as reorderable
chips, drag audio from the bay to append, x removes, "remove this block" clears.
moveOneshotBefore() handles reorder/move. Export packs one-shot audio + writes
type:"oneshot"; soundtrack lanes ignore one-shots (soundTracks() filter).

EXPORT (exportEski): packs pages as NNN.ext, unique audio once (music+oneshot),
writes manifest v2 + cover, downloads. Opus/WebCodecs compression was REMOVED
(too slow) — sources pack as-is.

TRASH BAR (#trash-bar): drag any bay block / page card / timeline band toward it
to delete (appears mid-drag, red when hovered). LOADING overlay (#loading) on
import/export. Undo/redo (ctrl+z/y), copy/paste (ctrl+c/v), delete, esc.

--------------------------------------------------------------------------------
7. LIBRARY  (index.html, the home page)
--------------------------------------------------------------------------------
Lists the library/ folder: tries the directory autoindex first (zero-config on
local dev / apache / nginx / python http.server / npx serve), falls back to
library/index.json (needed on GitHub Pages / Vercel). For each .eski it fetches
the file, JSZip-reads the manifest for title + cover (or first image), and builds
a cover grid. Clicking a cover opens a modal: cover, title, "read it" (opens the
reader via read.html?read=library/<file>), and a "discord" CTA
("find all these eskis and more in the discord channel"). DISCORD const at top.

--------------------------------------------------------------------------------
8. SPEC / SW / PWA
--------------------------------------------------------------------------------
spec.html: field-by-field manifest v2 docs incl. oneshot + oneshotLoop; ownership
rule, queues, silence, one-shots, v1 compat, deep links. Holds the real Gnomon
@font-face (source for injection into the other files).
sw.js: install caches the app shell (index/composer/library/spec/manifest/logo/
demo/jszip via allSettled), network-first for HTML, cache-first+refresh for
assets, CACHE='eski-v4'.
manifest.json: PWA (standalone, theme #5B7A6B, file_handlers for .eski via
launchQueue in the reader).

--------------------------------------------------------------------------------
9. TESTING  (Playwright smoke suite — ~135 assertions, zero-console-errors hard)
--------------------------------------------------------------------------------
The suite serves the project on localhost:8931, route-intercepts cdnjs (serves a
local JSZip + vendored pdf.js) and Google Fonts (empty), BLOCKS the service
worker, and drives the real UI. It also autoindexes a fixture library/ folder and
serves /fx/<name> raw fixtures for ?read= tests.

INSTALLED 2026-07-25: node_modules/ is present (playwright + jszip + chromium)
and .gitignore now keeps it, package.json, fixtures, vendor, dl, and *.eski out
of git. Suite is at 156 assertions and passes clean.
FIXED FLAKE: "reader: scroll mode" used to time out ~1 run in 4. Cause: it set
scrollTop = scrollHeight ONCE, but pages lazy-load, so scrollHeight was still
growing and the midline settled on an earlier page. The scroll is now re-applied
inside the waitForFunction poll. Four consecutive green runs after the fix.
If you write another scroll assertion, do the same: drive lazy-loaded scroll
positions from inside the poll, never once up front.

Setup (once), then run:
  npm i playwright jszip
  npx playwright install chromium
  node tests/vendor-pdfjs.js      # vendors pdf.js for offline runs
  node tests/make-fixtures.js     # builds test.eski/queue.eski/big.eski/oneshots.eski/cbz/pdf/wavs/library
  node tests/smoke.js
Generated fixtures/vendor/node_modules are NOT committed. Extend the suite per
feature; keep "zero console errors" a hard assertion. On this machine node may
need the full path: "C:\Program Files\nodejs\node.exe", and set
NODE_PATH to a node_modules that has playwright+jszip if running outside tests/.

Coverage today: reader boot/tint/deeplink/rtl/spread/jump/scroll/playlist/queue/
fullscreen/one-shots(down-up-loop-pause-playing-indicator-tutorial-hotkeys)/
big-page layout/zoom-pan/?read=; composer import(cbz/pdf/folderless)/default-
silence/bay/waveform/slip/grip/queue/undo-redo-copy-paste/zoom-minimap-fit/range-
select/pages reorder/settings drawer/export-as-is/icons/tray+page zoom/trash/
one-shots(block+panel+reorder+export)/dock search+filter; library folder+covers+
modal+read-it; service-worker not involved (blocked).

--------------------------------------------------------------------------------
10. DEPLOYMENT (currently GitHub + Vercel)
--------------------------------------------------------------------------------
- Ship the static app on Vercel. DO NOT put .eski media in the git repo.
- Host eskis + cover thumbnails on Cloudflare R2 (free egress ~ $0.015/GB-mo);
  point library/index.json entries at R2 URLs. (Storage is pennies; egress is the
  cost — R2/B2-behind-Cloudflare keep it ~$0. S3-style egress is ~100x pricier.)
- Vercel has NO autoindex: ship library/index.json (generate it in a build step
  that scans the library folder or R2 and also emits cover thumbnails, so the
  grid doesn't download whole eskis).
- vercel.json headers: no-cache for HTML/JS so deploys land (pairs with the SW
  fix); immutable 1y for content-addressed eskis/covers.
- Run the Playwright smoke suite in GitHub Actions on each PR; gate deploys green.

================================================================================
11. PENDING — URGENT IMPROVEMENTS  (requested; NOT yet built)
================================================================================
Priority next-session work. Grouped by app. (#0 was in flight and unfinished.)

#0 (in flight, reader): hide the standalone dark-mode toggle from the header and
   move it INTO the settings popover (reader) / settings drawer (composer) as a
   labeled toggle row; give it a different fitting icon (moon, not the contrast
   half-circle). Keep the 'd' hotkey. Update any smoke refs to #dark-btn.

READER:
R1. One-shot tutorial: show it ONCE EVER then never again. Currently it re-shows
    until dismissed and can reappear on later one-shot pages. Fix: set the
    localStorage seen-flag the moment it's first shown (+ auto-hide after a few
    seconds or on first one-shot trigger), not only on the X.
R2. One-shot ducking: while a one-shot is playing, DUCK the soundtrack volume a
    little (internal gain multiplier on audio.active), and restore when the
    one-shot ends/pauses. The user's VOLUME SLIDER must NOT move — ducking is a
    separate internal factor layered under audio.trackGain(), not the slider.

COMPOSER (big streamline — the soundtrack timeline goes away):
C1. Preview audio in the bay BEFORE dragging to the timeline: a play/stop control
    on each audio block (full playback, not a 2s snippet).
C2. Page INFO PANEL becomes the hub, and is PERSISTENT (a real docked panel, not
    the temporary center modal #qp). It shows, for the selected page:
      - the one-shot QUEUE (each row previewable end-to-end, full playback),
      - whether a SONG is triggered on this page (and its details/volume),
      - the page's SONG RANGE (which soundtrack owns it),
      - the song QUEUE if multiple share the trigger.
    Selecting a page in the grid shows its panel automatically — no need to click
    a per-page one-shot button to summon it.
C3. REMOVE soundtrack timeline mode entirely. Everything (one-shots AND songs)
    is authored from each page's info panel. Drag songs/one-shots/samples into
    the panel; set volumes there.
C4. The one-shot BLOCK no longer needs to be dragged onto a page. Just click a
    page -> its info panel -> drag in one-shots or audio samples. Remove the
    draggable one-shot tool.
C5. GRID (pages) view must show AT A GLANCE per page: has one-shots? triggers a
    song? is inside a song's page range? (badges/overlays on each page card).
C6. Composer PREVIEW pane needs ZOOM + PAN (reuse the reader's wheel-zoom /
    drag-pan) so you can actually read the dialogue on a page.
C7. Preview a page IN A READER inside the composer: actually hear the soundtrack
    + one-shots for the current page so you can balance global volumes. (Embed a
    minimal reader engine or reuse the reader's audio logic against cState.)
C8. Remove the DELETE button from the mini song player (clipbar) — deletion now
    happens in the page info panel.
C9. RESIZABLE left media bay: drag its right edge to resize, with a max width cap.
C10. 3-dots options menu on each media item in the bay. Must include TRIM for
     imported audio clips (set in/out on the raw sample). Add other useful ops:
     rename, duplicate, download, delete, "reveal usages".
C11. MULTI-SELECT with shift/ctrl (media items and/or pages) for batch ops.

(When building C2/C3: the manifest is unchanged — songs are still type music/
silence with sync.from ranges; one-shots type oneshot. Only the AUTHORING UI
changes. Keep the "reader overrides never mutate the manifest" rule. Keep
soundTracks()/oneshotsFor()/queueGroups() helpers; they already model the data.)

================================================================================
12. BACKLOG (condensed from TODO.txt — lower priority)
================================================================================
- [done] Discord invite wired: https://discord.gg/UFNpnHAbD
- Add a real demo.eski at the repo root.
- OG/meta tags so shared links unfurl (title + cover) in Discord.
- Externalize the Gnomon font to one gnomon.woff2 (it's base64-duplicated in 4
  files) so it caches once and shrinks every page.
- Library: pre-generated cover thumbnails + library/index.json metadata (title,
  creator, cover, pages, size, tags, added) so the grid stops downloading whole
  eskis; add search/sort; cache covers in IndexedDB.
- Position memory keyed by filename+title (not title alone) to avoid collisions.
- Reader loading spinner (parity with composer); global volume/mute memory.
================================================================
17. PERFORMANCE PLAN (2026-07-28). Measured, not guessed.
================================================================
The brief was "make the site fast: search, saving, loading, everything, and a
framework is fine if that is what it takes".

VERDICT ON A FRAMEWORK: no, and not close. Nothing slow here is caused by
hand-written DOM code. The pages are small (index 34 KB, studio 128 KB, tokens
13 KB) and render immediately. What is missing is a 20-line BUILD STEP, which
is a different thing from a framework. Revisit only if a second surface needs
real routing, or if the studio's state outgrows plain objects. Adopting React
today would add a toolchain and change nothing below.

WHAT IS ACTUALLY SLOW, in order of measured cost:

E1. THE AUTH CLIENT IS ON A THIRD-PARTY CDN, AND IT IS A SHIM.
    https://esm.sh/@supabase/supabase-js@2 returns 458 BYTES: a re-export that
    points at the real bundle. So every page that touches data makes two
    CHAINED requests to an origin we do not control before the first query can
    start, and platform.js gates everything behind that (window.eski.ready).
    This is why home feels slow with one comic on it: the wait is constant and
    has nothing to do with how much is on the shelf. It is also why the in-app
    browser shows "could not reach the shelf" whenever that host is blocked.
    FIX: vendor it. `npx esbuild --bundle --format=esm --minify` the client to
    vendor/supabase.js, import it relatively, and let it be cached with the
    rest of the app. One command in package.json, no framework, no dev server.
    This is the single biggest win and it also removes a third-party
    dependency from the critical path of every signed-in action.

E2. COVERS ARE FULL-SIZE PAGE SCANS.
    publish sets cover_key = parts[coverIndex].key, and parts are the ORIGINAL
    page blobs. Home then draws that multi-megabyte image into a 150px cell,
    and the reader's modal into 150px too. Two symptoms, one cause: the shelf
    is slow, and the cover looks bad because the browser is downscaling a tall
    page and object-fit:cover is cropping it to 2:3.
    FIX: generate a thumbnail at publish time in a canvas (about 400px wide,
    webp, quality ~0.82), upload it as its own content-addressed object, and
    store thumb_key alongside cover_key. Home and the modal use thumb_key; the
    comic page can use the full cover. Costs one extra small upload per comic
    and turns the shelf from megabytes into kilobytes.
    NOTE: this is also the honest fix for "the cover is low quality". Do not
    reach for a different crop until the thumbnail exists.

E3. HASHING BLOCKS THE MAIN THREAD.
    saveComic() runs crypto.subtle.digest over every page blob on the UI
    thread, then uploads four at a time. On a 40-page comic that is where the
    freeze comes from, before a single byte is sent.
    FIX: move hashing into a Web Worker, keep a real progress bar fed by
    completed bytes rather than file count, and let the UI stay live. True
    background upload that survives navigation needs a Service Worker with
    Background Fetch, which is Chrome-only; do not promise it cross-browser.
    Keeping the tab open with a responsive UI and a progress bar covers the
    actual complaint.

E4. NO AUTOSAVE, SO A CLOSED TAB LOSES EVERYTHING.
    FIX: snapshot cState plus the media blobs to IndexedDB on a debounce, and
    offer to restore on open. This is worth doing right after E3 because the
    same record makes an interrupted upload RESUMABLE: content-addressed keys
    mean an object already in the bucket never needs sending twice.

E5. NO CACHE HEADERS.
    Media keys are already content-addressed, so they can be immutable for a
    year, and HTML must stay no-cache so deploys land. vercel.json has an
    installCommand and nothing else.

E6. URLS.
    A comic and a profile have no addresses of their own (B3, B4). Vercel
    rewrites can give /c/<slug> and /@<handle> without a router.

E7. SEARCH IS NOT A PROBLEM YET, and saying so is part of the plan. It filters
    an already-loaded array client side, which is instant into the thousands.
    When the shelf outgrows one query, move to Postgres full text or trigram
    and paginate. Do not build that now.

ORDER: E1, E2, E3, E4, E5. E1 and E2 are most of the felt slowness and neither
touches architecture. E6 rides along with the comic page.

================================================================
RECENT WORK NOT PREVIOUSLY RECORDED HERE (2026-07-28)
================================================================
- Sign in is live (Google). Discord is enabled per-provider in supabase and is
  one word in PROVIDERS (platform.js) once it is switched on there.
- Publishing works end to end: studio -> presigned R2 PUTs -> comics/pages/
  tracks rows -> home. schema.sql is applied.
- profile.html: handles, bio, avatar, share link, and four shelves (published,
  drafts, contributed to, saved). schema-profiles.sql adds profiles and the
  comics.description column. Contributed and saved are honest empty states
  until the parts and saves tables exist.
- Drafts: saveComic(status) is one path; a draft simply does not flip status.
  A server draft CANNOT be reopened in the studio yet (see E4).
- The export button is gone from the studio; exportEski() remains for the
  preview pane and the suite. Home no longer shelves files.
- Error reporting: toasts stay until dismissed, api/sign.mjs answers in json
  and sanitises env vars pasted as whole KEY=value lines, comicFromApi names
  which step failed, and a page image that 404s names its url.

================================================================
BEFORE A SECOND PERSON ARRIVES (added 2026-07-28, owner approved)
================================================================
These are not features. They are the things that break, cost money, or create
liability the moment someone who is not the owner uses the site. Ranked: the
first two get materially harder to add after people have published.

B1. UPLOAD QUOTA ON THE SIGNER. api/sign.mjs will sign up to 500 urls per call
    for ANY signed-in user, with no per-user limit and no size cap, and a
    presigned PUT cannot enforce content-length. One script fills the bucket
    and the bill. Minimum: a per-user daily object count and a max page count
    per comic, checked in the function before signing.
B2. HANDLES. comics.owner_name is a denormalized display name from Google. Two
    people called "Alex" are indistinguishable and there is no stable string
    for a profile url. Any identity beyond one user needs a unique handle, and
    retrofitting one after publishing is worse than adding it now.
B3. COMIC PAGE (P-1). A comic has no url of its own: home opens a modal, so a
    link shared in the discord lands on the shelf instead of the thing. This is
    the arrival path for every new reader, and it is where the OG tags belong.
    The modal already holds the content.
B4. PUBLIC PROFILE. profile.html is the OWNER's dashboard (drafts, delete,
    unpublish). There is no page for "who made this", so clicking an author
    name does nothing and attribution is just a string. Same file can serve
    both, split on whether the visitor owns it. Needs B2.
B5. TERMS, PRIVACY, TAKEDOWN CONTACT. The moment someone else uploads, the
    owner is a host rather than an author. Google's OAuth consent screen wants
    a privacy policy link for a published app (unverified caps around 100 users
    and shows a warning), and safe harbour depends on a stated way to receive
    infringement notices. Not legal advice; "no page at all" is still clearly
    wrong once D1 exposure moves to other people's work.
B6. REPORT NOTIFICATION. The owner is the sole moderator and nothing announces
    a new row, so reports rot until someone looks. One discord webhook.
B7. ACCOUNT DELETION. Supabase gives users no self-delete, so today it is a
    manual dashboard job. Someone will ask.

- SAVE DRAFTS (owner asked for this 2026-07-28): a work in progress should
  survive closing the tab. Local first (IndexedDB, the autosave below), then
  server side once accounts exist, so a draft follows you between machines.
  The comics row already has status 'draft', so the server half is mostly a
  publish flow that stops before flipping status.
- Composer: autosave draft to IndexedDB; re-add a pre-export validation strip
  (duplicate triggers, tracks past last page, zero pages); export progress %.
- Perf: LRU-revoke off-screen blob URLs; createImageBitmap; blurhash placeholders.
- Accessibility: aria-labels on icon-only buttons.
- Later one-shot ideas: polyphonic mode (already have monophonic).

================================================================
18. ERROR CODES (2026-07-30). Every external-service failure names itself.
================================================================
Full table: ERRORS.txt. Guarded by tests/errors.js.

Anything that talks to supabase, vercel, R2 or the browser's own storage now
answers with an ESK-#### code first: 1xxx boot, 2xxx auth, 3xxx the signer and
its env vars, 4xxx uploading, 5xxx the database, 6xxx reading media back, 7xxx
local storage. The code names the exact call site, so a pasted toast is enough
to find it. Codes are stable; retire one rather than renumber it.

Three things carry the design:
- api/sign.mjs answers { code, error } on every refusal, and the error string
  starts with the code, so both a machine and a person can read it.
- platform.js owns DB_HINTS, which translates the postgres and postgrest codes
  worth knowing (42P01 table missing, 42703 column missing, 42501 RLS, PGRST301
  expired jwt, 23505 taken) into the fix. It lives there because platform.js is
  on all four pages; window.eski.dbError(code, what, err) is the shared call.
- boot failures are separated: 1001 the vendored client did not load, 1002
  createClient threw, 1003 the url or key is unset, 1004 supabase is
  unreachable. Each has a different fix and they used to be one message.

FIXED WHILE WIRING THIS, and it was a real bug: platform.js is a MODULE, so it
executes AFTER a classic script has parsed. profile.html called load() at parse
time, read window.eski before it existed, and told everyone "could not reach the
server" on a perfectly healthy site. index.html and read.html were racing the
same thing and only won by accident (an awaited indexeddb call happened to yield
first). All three now wait for the module: modules run before DOMContentLoaded,
so that event is the gate. studio.html awaits window.eski.ready in saveComic
rather than reading a client that may not have booted yet.
If the reported auth flakiness had one cause, this was it.

================================================================================
14. THE PIVOT, DECIDED  (supersedes section 13's open questions; 2026-07-25)
================================================================================
Section 13 laid out the shape. This section is the settled version: decisions
made, data model, build order. Where 13 and 14 disagree, 14 wins. Section 11
(M1-M8, C1-C11) is unchanged and folded into the build order below.

--------------------------------------------------------------------------------
14.1 TERMINOLOGY (use these words, nothing else)
--------------------------------------------------------------------------------
COMPOSER    the person who scores a comic. Was "musician" in earlier drafts.
STUDIO      the authoring app, studio.html. Renamed from composer.html on
            2026-07-25 to free the word "composer" for the person. A composer
            is a human, the studio is a tool. Never call the app a composer.
BASE        the comic itself: pages, cast, slots, consent. Owned by the author.
SLOT        an authored void on a page: a dialogue line or an SFX hit. No audio.
VO          one voice actor's full set of clips for ONE character on one base.
SOUNDTRACK  one musician's music/silence tracks + audio for the base's SFX slots.
MIX         base + one VO per character (or muted) + one soundtrack (or none).
            The thing a reader actually loads and a view is logged against.
CANONICAL   the author-pinned default mix. Required at publish.

Retired words: "cut" (now MIX), "performance" (now VO), "sound package" (now
SOUNDTRACK), "musician" (now COMPOSER), "one-shot" stays only as the
reader-engine/manifest term.

--------------------------------------------------------------------------------
14.2 DECISIONS (settled, do not relitigate)
--------------------------------------------------------------------------------
D1  ORIGINAL WORK ONLY. eski hosts comics whose author is on the platform and
    can actually grant consent. No fan-dub-of-third-party-manga business. The
    copyright/DMCA exposure is what kills this project otherwise. (Absolute
    Batman in library/ is local test content only, never published.)
D2  BASE IS IMMUTABLE ONCE PUBLISHED. There is no version 2 of a comic. Only
    errata: swap a page IMAGE in place, same page count, same slot ids. Reason:
    any reflow destroys every VO and soundtrack built against it. This kills the
    comic_versions table, the slot-migration story, and the orphan problem all
    at once. If a comic genuinely needs restructuring it is a NEW base.
D3  FULL COVERAGE VOs ONLY. A VO stays draft until every line slot for its
    character has a clip. Publish is blocked below 100%. Composer shows a
    coverage meter. Kills partial-VO ranking abuse.
D4  NO LEADING DEAD AIR. Enforced at ingest, not left to the VA. decodePeaks
    already decodes every file; find first sample above the noise floor and
    auto-set trim_start_ms there. lead_ms clamped to 0-250. VA nudges inside
    that window only.
D5  NO AUTHOR APPROVAL GATE. VOs and soundtracks publish directly. Readers
    combine freely. Quality is sorted by popularity; abuse is handled by reports
    to mods. MODS = THE OWNER, SOLE MODERATOR, confirmed. Author still pins a
    canonical mix, which is
    also the cold-start default when nothing has plays.
D6  CONSENT, TWO AXES, THREE MODES EACH:
      voice_consent  : closed | allowlist | open
      music_consent  : closed | allowlist | open
    Allowlist is per-axis, per-user. Goal is that authors run open (more
    exposure, and payouts later), but closed/allowlist exist.
D7  REVOCATION IS PROSPECTIVE. Flipping open -> closed does NOT kill existing
    published VOs/soundtracks. Only a moderator takedown removes work. Otherwise
    nobody invests effort in a dub.
D8  PAYOUTS AND SPLITS: DEFERRED. Not built, not designed. BUT log views and
    resolved contributors from day one of multi-user; attribution history cannot
    be backfilled.
D9  CONTRIBUTORS SEE THE FULL ART. Accepted risk. Consent gates attaching work,
    not redistribution. No technical fix exists.
D10 COMPOSER MODES. See 14.4. Musicians see music + SFX. VAs see dialogue, and
    ALSO SEE SFX SLOTS READ-ONLY (they hear canonical SFX for timing, they
    cannot fill them). ASSUMPTION FLAG: "let VOs see the sfx" was read as
    read-only context. One flag flips it to fill rights if that was wrong.
D11 HOME IS THE LIBRARY, not the reader. The old eski explainer is gone,
    replaced by a short once-ever onboarding popup explaining the platform.
D14 LOCAL SHELF (owner staging, pre-backend). The library page accepts dropped
    .eski files and keeps them in indexeddb (db 'eski-shelf', store 'files'),
    showing them in the same grid with a "shelved" tag and a remove button.
    They open as ?read=idb:<id>, which the reader resolves via shelfBlob().
    This does NOT violate D13: the reader still only opens what the library
    hands it. Replace with real uploads at step 11; the shelf is a local
    staging area, not a feature to advertise.
D13 CLOSED ECOSYSTEM. The reader opens ONLY what the library hands it via
    ?read=. Removed: the paste-a-URL fetch box, "choose a file", drag-and-drop
    onto the viewer, the "new file" player-bar button, and the PWA .eski
    file_handlers in manifest.json. loadFile()/loadFromUrl() are gone; boot
    fetches ?read= (or demo.eski) and nothing else. The empty state points at
    the library. Every screen must keep reflecting this: no surface should
    advertise opening arbitrary files. THE COMPOSER IS THE EXCEPTION, authors
    obviously still import their own pages and audio.
D12 THE THREE APPS STAY THREE APPS. No separate VO booth, no separate musician
    tool. The composer in a MODE is both. It already has drag-drop import,
    waveforms, trim, preview, undo.
D18 THE UI IS SWISS GRID IN SAGE (2026-07-28). One hue, square corners,
    hairline dividers instead of shadows, Jost only with tabular figures for
    readouts, Gnomon for display above 32px, and density treated as a feature
    because this is a content platform. Tokens live in tokens.css; the full
    system, the product vocabulary and the tripwire list live in the
    .claude/skills/eski-brand skill, which REPLACES the old sage marketing
    guide (that one had no product vocabulary: no table, toolbar, panel or
    dense list, and a block rhythm built for a landing page).
    Structure borrowed from the brutalist skill: grid-gap hairlines, square
    geometry, bimodal density. Its red accent, uppercase type, ASCII framing
    and scanline/noise filters were rejected: the content here is other
    people's artwork and nothing may sit on top of it.
    THIS REVERSES the studio's "depth over brand purity, by owner request"
    (section 6). --shadow and --shadow-lg are now `none` everywhere.
    HOW IT WAS DONE CHEAPLY: the four pages had grown two colour vocabularies
    (--bg-0..3/--edge/--shadow vs --paper/--surface/--line). Both are defined
    in tokens.css in terms of the sage ramp, so all four repainted at once
    without touching 847 lines of component CSS.
D17 THE ZIP IS A TRANSPORT FORMAT, NOT THE BACKEND'S STORAGE UNIT (2026-07-27).
    Platform content lives as LOOSE CONTENT-ADDRESSED OBJECTS on R2 (one object
    per page image, per audio clip, per cover) with metadata in Postgres. The
    .eski zip survives everywhere it is already good: studio export, offline
    reading, handing someone a file in Discord, the local shelf, studio
    round-trip import. The reader keeps BOTH loaders permanently.
    WHY: the zip's "lazy loading" is lazy DECOMPRESSION, not lazy DOWNLOAD.
    Boot fetches the entire blob before JSZip.loadAsync, so a 66MB comic is
    ~25s of blank screen on a 20Mbps line before page 1 paints. Loose objects
    make that one image. It also makes D2 errata a one-object swap instead of a
    66MB re-upload that busts every cache, gives covers to the library without
    downloading whole comics (kills that backlog item), makes
    Cache-Control:immutable honest because keys are content-addressed, and lets
    a VO's ~200 clips upload browser->R2 by presigned PUT with no server-side
    unzip (R2 has no compute).
    COST: N requests instead of 1 (noise over HTTP/2 to a CDN), and the "a base
    is one atomic file" property, which the comic row replaces and does better:
    a half-uploaded zip is corrupt, a half-uploaded object set is just a row
    that never flipped to published.
    THE PORT IS NEARLY FREE: ensurePageUrl() short-circuits on p.url
    (read.html:634) and audio.resolve() on track.url (read.html:1030), so
    platform content that arrives as urls needs no engine change. All that is
    needed is a comicFromApi() sibling of comicFromZip() filling the same
    object shape with urls instead of zip entries.

--------------------------------------------------------------------------------
14.3 THE FOUR PEOPLE
--------------------------------------------------------------------------------
READER. Opens library (home). Picks a comic. Gets the canonical mix by default,
  or opens the mix picker: choose a VO per character (or mute), choose a
  soundtrack. VO preview lets them audition before committing (see 14.6). Read
  loop is unchanged: pages turn, music follows, down/up walks the lines.
  The reader never knows it assembled three sources.

AUTHOR. Imports pages (existing composer import). Then two author-only jobs:
  CAST, e.g. [{key:"aiko", name:"Aiko"}] - only the author knows who speaks.
  SLOTS, marking every dialogue bubble and SFX hit: page + order + kind +
  cast key + bubble rect + optional attachTo/offset. No audio attached.
  Then sets consent, publishes, pins a canonical mix.

MUSICIAN. Browses bases where music_consent allows. Sees page thumbnails, page
  count, slot layout for timing. Drags music, sets trigger pages, fills the
  author's SFX slots. Exports a soundtrack bound to that base.

VOICE ACTOR. Browses bases where voice_consent allows. Picks ONE character.
  Sees only that character's line slots, in page order. Per slot: a CROP of the
  bubble rendered from the page image (this is why rect exists), the previous
  line played back for pacing, drag-and-drop import of externally recorded audio,
  trim, retake. Publishes at 100% coverage.
  NOTE: this is why section 13's "no captions/transcripts" holds. Correct for
  readers (the text is on the page). The VA gets the bubble crop instead of a
  transcript field. Costs nothing, art is already loaded.

--------------------------------------------------------------------------------
14.4 COMPOSER MODES  (cState.editMode)
--------------------------------------------------------------------------------
Chosen when the composer opens. 'scratch' only when starting from nothing.

                          scratch    vo        soundtrack
  pages add/remove/order    yes      no          no
  cast + slot marking       yes      no          no
  consent settings          yes      no          no
  fill LINE slots           yes      yes         no
  see SFX slots             yes      read-only   yes
  fill SFX slots            yes      no          yes
  music/silence tracks      yes      no          yes
  media bay shows           all      audio       audio
  export target             base     vo          soundtrack

scratch has no restrictions: that is how one person builds a complete demo eski
end to end (base + VO + soundtrack in one pass) with nobody else involved.

Post-C3 all authoring happens in ONE page info panel, so mode gating is mostly
filtering that panel plus reusing the existing bayFilter. Not scattered.

SECURITY: mode gating is UI only. The server must independently validate that a
VO submission touches only line slots for its own character on the stated base.
Never trust a client-side mode flag.

--------------------------------------------------------------------------------
14.5 DATABASE SCHEMA
--------------------------------------------------------------------------------
Postgres (Supabase: auth + db + row-level security, which kills the whole
"build accounts" phase). Media bytes on Cloudflare R2 via presigned PUT,
content-addressed keys, immutable cache headers. Metadata in Postgres, bytes in
R2, never mixed. No comic_versions table: see D2.

  users             (id, handle, display_name, created_at)

  comics            (id, owner_id, title, slug, direction, page_count,
                     cover_key, status, published_at, created_at)
  pages             (id, comic_id, idx, image_key, w, h,
                     note text)            -- author direction for this page
  cast_members      (id, comic_id, cast_key, name, note,
                     kind 'character'|'narrator')
  slots             (id, comic_id, page_id, order_idx,
                     kind 'line'|'sfx',
                     trigger 'user'|'page'|'attached',
                     cast_member_id,
                     rects jsonb,          -- OPTIONAL, see 15.1
                     attach_to_slot_id, offset_ms)

  consents          (comic_id, axis 'voice'|'music',
                     mode 'closed'|'allowlist'|'open', updated_at)
  consent_allow     (comic_id, axis, user_id)

  vos               (id, comic_id, artist_id, cast_member_id, title,
                     gain_db, status 'draft'|'published', preview_key,
                     filler bool,          -- timing reference only, see 15.4
                     explicit bool,        -- uncensored delivery, see 15.6
                     plays, created_at)
  vo_clips          (id, vo_id, slot_id, audio_key, duration_ms,
                     trim_start_ms, trim_end_ms, lead_ms, gain_db)

  soundtracks       (id, comic_id, artist_id, title, gain_db,
                     status, plays, created_at)
  soundtrack_tracks (id, soundtrack_id, order_idx,
                     trigger_page_id, trigger_slot_id,   -- one or the other
                     type 'music'|'silence', audio_key,
                     volume, start_ms, end_ms, gain_db)
  soundtrack_cues   (id, soundtrack_id, audio_key, gain_db,
                     slot_id,                    -- fills an author sfx slot
                     attach_to_slot_id, offset_ms) -- or the musician's own cue

  mixes             (id, comic_id, combo_hash unique, soundtrack_id,
                     is_canonical, plays, created_at)
  mix_vos           (mix_id, cast_member_id, vo_id)   -- null vo_id = muted

  reports           (id, target_type 'vo'|'soundtrack', target_id,
                     reporter_id, reason, status, created_at)
  takedowns         (id, target_type, target_id, mod_id, reason, created_at)

  views             (id, mix_id, viewer_id, session_id,
                     started_at, pages_read, completed)
  view_contributors (view_id, user_id, role, weight)

STORE OBJECT KEYS, NEVER FULL URLS. Every *_key column holds the R2 path
("a/3f9c2e/01.mp3"), and the reader builds R2_PUBLIC_BASE_URL + '/' + key at
request time. This keeps the media host a one-variable decision: ship on the
rate-limited pub-<hash>.r2.dev dev domain now, move to cdn.eski.lol whenever
the nameservers land on Cloudflare, flip one env var, migrate zero rows.
Storing full URLs turns that same switch into a rewrite of every media row.
(Nameserver note: an R2 custom domain REQUIRES the eski.lol zone on Cloudflare.
That is the only real reason to move it. If you do, the apex and www records
must stay DNS-only/grey-cloud pointing at Vercel; proxying Cloudflare in front
of Vercel stacks two CDNs. Env vars are unaffected by any DNS change.)

Three things worth defending:
- SLOTS ARE REFERENCED BY ID, NEVER PAGE INDEX. Index-based references break the
  moment anything shifts. (D2 means nothing shifts, but ids cost nothing and
  survive the day D2 gets revisited.)
- MIXES ARE UPSERTED BY combo_hash. Readers combine freely (D5), so a mix row is
  materialized on demand, not curated. Popularity then accrues per VO, per
  soundtrack, AND per whole combination independently.
- view_contributors IS WRITTEN AT VIEW TIME, DENORMALIZED. Mix composition
  changes later. Payout math must never re-resolve history.

--------------------------------------------------------------------------------
14.6 LOADING
--------------------------------------------------------------------------------
  GET /api/mix/:id  ->  ordered page urls + resolved track list (audio urls)

REVISED BY D17: there is no base zip on the platform. One request returns both
lists, built from R2_PUBLIC_BASE_URL + the object keys held in Postgres.
- PAGES: individual R2 objects in pages.idx order. The reader keeps
  prefetchAround(+/-3); only the source of each url changes.
- AUDIO: individual R2 objects, NEVER zipped. A VO zip forces downloading all
  200 lines before page 1 renders. Individual objects prefetch per page the way
  images already do. Lines are 50-200KB.

THE REASON THIS IS CHEAP: ensurePageUrl() returns p.url when set
(read.html:634) and audio.resolve() returns track.url when set
(read.html:1030); both only fall back to a zip entry. Platform content simply
arrives carrying urls. The reader engine needs ZERO structural change. The mix
resolver is comicFromApi(), a ~40 line sibling of comicFromZip() that fills the
same object shape, in front of the existing openComic().

The manifest stops being a file and becomes a response. Same shape, new source.
comicFromZip() stays forever for export, offline, the local shelf and studio
round-trip import.

VO PREVIEW: generated at ingest, not assembled at browse time. Take the VO's
FIRST 3 SLOTS in the comic, concatenate to one clip, store as preview_key.
Deterministic, needs no "densest page" computation, and the opening lines are
what a reader wants to audition anyway. Browsing a VO then costs one small
fetch instead of resolving a whole mix.

EXPORT STILL WORKS: flatten a mix to a portable single .eski via the existing
exportEski. Offline/sharing snapshot, no format schism.

--------------------------------------------------------------------------------
14.7 VOLUME NORMALIZATION
--------------------------------------------------------------------------------
Measured at INGEST, reusing composer's decodePeaks (it already runs
decodeAudioData for waveforms, so this is nearly free). Store a gain. Never
touch samples, never re-encode: there is no encoder left (opus was removed).

NORMALIZE PER VO / PER SOUNDTRACK, NOT PER CLIP. Per-clip is the trap: a
whisper and a scream both land at -16 LUFS and the performance is destroyed.
Compute ONE gain from the artist's aggregate loudness. That matches VA to VA
and soundtrack to soundtrack while preserving dynamics inside each one.

TWO TARGETS, NOT ONE: dialogue ~-16 LUFS, music ~-20 LUFS. Music sits under by
construction, so the duck has less work to do and pumps less.

TRUE PEAK: after gain, check peak; if >0.99 clamp the gain down.

RUNTIME CHAIN:
  clip gain (manual) x vo/soundtrack norm x user volume x mute -> element.volume
                                                              -> duckGain node
                                                              -> peaking EQ
                                                                 400Hz, 2kHz
trackGain() in index.html is the single choke point; the norm multiplies there,
one line, and covers soundtrack and one-shots both.

--------------------------------------------------------------------------------
14.8 TECH STACK
--------------------------------------------------------------------------------
  reader / composer / library     vanilla, no build. Unchanged. Works.
  slot marking + soundtrack auth  INSIDE the existing composer (modes)
  VO booth                        INSIDE the existing composer (vo mode)
  dashboard (cast/consent/mixes)  DEFER THE FRAMEWORK
  auth                            Supabase Auth, GOOGLE + DISCORD OAUTH
                                  (owner decision 2026-07-27, supersedes the
                                  earlier email-magic-link call and the earlier
                                  rejection of Discord). No email path at all,
                                  so no SMTP dependency and no "check your
                                  email" screen: P-6 in 16.2 is dead.
                                  Lives in platform.js, loaded by index/read/
                                  studio. Fails soft: no client, no auth ui,
                                  page unchanged. The smoke suite takes that
                                  path via an esm.sh route that throws.
  db                              Supabase Postgres + RLS (RLS enforces consent
                                  at the query layer, not in app code)
  media                           R2, presigned PUT, content-addressed
  host                            Vercel (already there)
  domain                          eski.lol
  supabase project                https://zidqagrmxeawpasurpwi.supabase.co
  r2 bucket                       eski
  media base url                  https://pub-b9e7c6b680ca415e9ffd5875bad0df03.r2.dev
                                  (r2.dev dev domain, rate limited. swap to
                                  https://cdn.eski.lol when the zone moves to
                                  cloudflare: one env var, zero row migrations)
  env vars                        see .env.example; r2 cors in r2-cors.json
  discord                         https://discord.gg/UFNpnHAbD (wired in index.html
                                  DISCORD const + read.html header/footer)

Section 3's "vanilla JS only" rule stays SCOPED to the three apps. It is lifted
for the platform layer. But do not pick a framework yet: Supabase's JS client
plus vanilla covers everything until a second surface needs real routing.
Picking Next/SvelteKit now buys nothing and costs the no-build-step property.

--------------------------------------------------------------------------------
14.9 BUILD ORDER  (biggest visible change first; multi-user work last)
--------------------------------------------------------------------------------
 1. HOME BECOMES LIBRARY. git mv index.html read.html; git mv library.html
    index.html. Font blocks travel with the files, no re-injection needed. Then
    fix: sw.js ASSETS + offline fallback, manifest.json start_url/file_handlers,
    library read-it links, spec.html footer, nav across all files, smoke URLs.
 2. ONBOARDING POPUP. Delete the reader's #about explainer. Once-ever modal on
    the library: comic + soundtrack + VO, and how to contribute. Reuses the
    existing #tut-os / #keys-panel CSS pattern.
 3. #0 dark toggle into settings; R1 one-shot tutorial once-ever.
 4. [DONE] AUDIO MIX, M1-M7. Shipped: graph built in unlockAudio (DUCK table,
    buildGraph/resumeGraph in read.html), duck keyed on setOsPlaying with a
    600ms release, per-one-shot "duck" override, "duck under voices" row in the
    settings popover, volume-only fallback via duckMul in trackGain, and
    gainDb normalization measured in composer decodePeaks + written by
    normGains() at export (music per track, one-shots share one gain).
    spec.html documents player.duck, track.duck, track.gainDb.
    M8 DONE TOO: the suite asserts the graph is built on both elements, the
    duck engages/holds across a requeue/releases on a debounce, the off switch,
    the per-one-shot override, the no-web-audio fallback, and gainDb math.
    (original scope, for reference) AUDIO MIX, M1-M8. Most audible change in the project. Web Audio graph on the
    a/b elements ONLY (os.el stays unrouted so one-shots are never ducked).
    CREATE THE SOURCE NODES IN unlockAudio(), NOT AT BOOT: createMediaElementSource
    reroutes output exclusively into the graph, so a suspended context = silence.
    Once-only still holds since a/b are never recreated. Duck keys on
    setOsPlaying with a ~600ms release debounce, which gives "one duck per run
    of queued lines" for free and also stops attached SFX double-ducking later.
 5. [DONE] COMPOSER PAGE PANEL: C1, C2, C5, C6, C9.
    C2: #qp is now a DOCKED, PERSISTENT third column in .work (no scrim, no
      modal). openQueue(page) is the "select this page" entry point and is
      still the name every call site uses. renderInfo() paints the soundtrack
      section (which queue owns the page, whether it triggers there, per-track
      volume + preview + remove) and then renderQueue() for the one-shots.
      groupOwning(page) is the new helper.
    C1: previewEl, a single shared <audio>, gives full-length audition from
      both the media bay chips and the panel rows. previewKey tracks what is
      playing so a second press stops it.
    C5: page cards show a "trig" badge (song starts here), an "inrange" colour
      stripe (inside a song's range), the existing one-shot block, and .sel for
      the selected page.
    C6: pvZoom/pvX/pvY + pvZoomAt/pvApply on #preview, wheel + drag + dblclick,
      reset on page change. Same feel as the reader.
    C9: #bay-resize handle, pointer drag, clamped 160-420px.
    GOTCHA HIT WHILE BUILDING THIS: renderPages() already had `const g` for the
    grid element, so a `const g` for the owning group shadowed it and broke
    g.appendChild. The group variable is `grp`.
    STILL OPEN from the C list: C7, C8, C10, C11 (see step 9).
 6. [DONE] C3 + C4: THE SOUNDTRACK TIMELINE IS GONE. composer.html went from
    1593 to ~1450 lines and the whole timeline vocabulary is deleted:
    renderTimeline, pxPerSec, drawBandWave, attachGrip, attachSlip, scrub,
    renderClipbar, minZoom, zoomFit, zoomStep, syncMinimap, the minimap drag,
    setTLMode, pageAtClientX, laneOf, laneCount, laneH, and the tlMode / zoomX /
    laneHeight / selRange state. The ruler, page row, lanes, bands, sel-band,
    mode switch, clip bar and #os-tool markup and css all went with them.
    WHAT REPLACED EACH AFFORDANCE (all in the page panel now):
      grip drag (trigger page)  -> "starts p." number input, and the panel
                                   follows the song to its new page
      slip drag (in point)      -> "in" seconds number input
      clip bar volume           -> "vol" slider on the song row
      clip bar rename           -> the song row name is an input
      clip bar +queue           -> drop a second audio on the same page
      clip bar remove           -> trash button on the song row
      add silence               -> button at the bottom of the soundtrack section
      2s scrub preview          -> full-length preview (C1) on the row
      band drag to trash        -> row trash button
      ruler drag (page range)   -> gone; qpPage is the single selection concept
    DROP TARGETS ARE NOW SCOPED: #info-song takes audio and starts/queues a
    song, #qp-list takes audio and makes a one-shot, page cards still take
    audio as one-shots so the common case needs no aiming.
    sel survives as the copy/paste/delete selection, set by clicking a song row.
    Suite rewritten to match and is at 163 assertions, green twice.
 7. [DONE] CAST + SLOTS + STUDIO MODES. Cast list, slot marking with bubble rects,
    editMode gating, coverage meter (D3), leading-silence trim (D4). Still pure
    local files, no backend.
 8. [DONE] SEPARATE .eski PARTS + READER MIX PICKER. Base/VO/soundtrack as distinct
    exports; reader loads a base and accepts VO/soundtrack files alongside,
    merges manifests, plays the combination.

    *** STOP AND LOOK HERE. After 8, with ZERO BACKEND, one person can author a
    base, record a VO, score a soundtrack, and read the assembled mix. The
    entire platform model is proven on files. Everything after this is hosting
    and discovery, not architecture. If the model is wrong, it fails here,
    cheaply. ***

 9. [MOSTLY DONE] CLEANUP.
    C7 DONE: previewInReader() packs the current state to a blob and hands it
      to read.html in an iframe, so the preview is the REAL reader engine
      (ducking, chains, queues) instead of a second implementation that would
      drift. Closing sets the frame to about:blank so its audio stops.
    C8 DONE: fell out of the timeline deletion, the clip bar no longer exists.
    C10 DONE for in/out points: every track carries start and end, exported as
      sync.start / sync.end, and one-shots honour an out point via a timer.
      Non-destructive by design; there is no encoder and adding one would mean
      shipping wav. What is NOT done: a waveform scrubber to set the points by
      dragging, and the 3-dots media menu (rename, duplicate, reveal usages).
    C10 FULLY DONE 2026-07-25: 3-dots menu on every media item (trim, rename,
      duplicate, download, reveal usages, delete) plus a waveform trim popover
      with draggable in/out handles. Trim sets points on the SAMPLE and every
      track already using that clip picks them up, because that is what a user
      means by "trim this clip". Still no re-encode, ever.
    C11 DONE: shift/ctrl multi-select over media items and page cards,
      Delete acts on the whole picked set.

D15 NO SPREAD MODE (2026-07-25). Removed entirely: the button, the 's' hotkey,
    the two-image layout, toggleSpread, and the leftmost-page audio rule.
    Reason: eski is original-work-only (D1) and digital-first, so a two-page
    spread is an ARTWORK decision the artist exports as one wide page, not a
    reading mode the app has to model. Scanned print comics were the only real
    use and they are not what this platform hosts.
    This also deletes the bug where a spread hid half the dialogue, because
    os.list was built from the leftmost visible page only. audioPage() is now
    literally `return currentPage`. Do not reintroduce it.

D16 MOBILE IS A FIRST-CLASS TARGET (2026-07-25). Every surface must be fully
    usable at 9:16 (tested at 390x693), no horizontal scrolling anywhere, and
    every tap target at least 40px.
    - LIBRARY: two-column grid, icon-only nav, modal scrolls and fits, remove
      buttons always visible since there is no hover on touch.
    - READER: the player bar wraps and its pills go icon-only (the labels are
      bare text nodes, so font-size:0 on the button and a restored size on the
      .ico is what collapses them; title= still reads out). Settings and the
      hotkeys panel become bottom sheets. THE SHEET COVERS THE BUTTON THAT
      OPENED IT, so it carries its own close row (.sheet-head, hidden on
      desktop). Click zones are 22vw for thumbs.
    - STUDIO: the three-column desk cannot survive 9:16, so the media bay and
      the page panel become off-canvas drawers behind a scrim, toggled from the
      header, and the stage keeps the full width. Selecting a page auto-opens
      the panel. The bay resize handle is disabled there.
    CSS ORDER GOTCHA, COST AN HOUR: the mobile @media block MUST sit at the END
    of the stylesheet. Put it earlier and later plain rules with equal
    specificity win, so drawers silently refuse to slide in.
10. [DONE] ADDITIVE MANIFEST: SFX-attach (role/attachTo/offset), meta.chapters,
    meta.series, meta.credits. Credits gives real attribution with no backend.
--- everything below needs a second human to mean anything ---
11. BACKEND. Supabase auth + Postgres + RLS, R2 presigned upload, publish and
    browse endpoints, server-side validation of submissions (see 14.4 SECURITY).
    Library reads the API instead of index.json.
12. CONSENT + MODERATION. Two axes, three modes, allowlist, reports.
13. POPULARITY + VO PREVIEW + MIXES. Ranking, preview clips, combo_hash upsert,
    canonical pin.
14. VIEWS + view_contributors. Even with splits deferred (D8). One insert, and
    it cannot be backfilled.

--------------------------------------------------------------------------------
14.10 KNOWN BLIND SPOTS (accepted or open)
--------------------------------------------------------------------------------
- Steps 1-2 make eski LOOK like a platform long before step 8 makes it BEHAVE
  like one. Fine while it is one user. Do not announce at step 2.
- Recording quality floor: MediaRecorder is webm/opus and iOS Safari is quirky;
  untreated bedroom audio next to produced music sounds broken. D4's ingest pass
  is the right place to also high-pass and check the noise floor, and warn.
  Otherwise the duck and EQ get blamed for problems they cannot fix.
- Line pacing: a VA records in isolation and the reader triggers manually, so
  delivery can feel disjointed. Mitigation lives in the booth (play previous
  line, show the page), not in the format.
- SFX timing chicken-and-egg: a musician filling SFX slots that attach to a line
  cannot hear that line if no VO exists yet. Either score against canonical, or
  attach-offsets get authored blind. Decide before building SFX-attach (step 10).
- Popularity ranking is gameable and rich-get-richer. Irrelevant at one user.
- Reports need a mod queue eventually. Table is enough for now, skip the UI.
- Storage: a 40-page comic + ~200 lines x 3 VOs + a soundtrack is roughly
  200-400MB. R2 storage is pennies, egress free. Not a constraint.
- Splits (D8) are the first thing contributors will ask about. The data model
  makes it answerable; it does not answer it.

================================================================================
15. DIALOGUE MODEL  (how comic dialogue actually maps onto slots; 2026-07-25)
================================================================================
Worked through the real conventions of comic lettering and what each one demands
of the schema, the reader engine, and the composer. Section 14's schema block is
already updated to match. Decisions here are settled unless marked OPEN.

--------------------------------------------------------------------------------
15.1 THE CENTRAL RULE: A SLOT IS A RUN OF SPEECH, NOT A BALLOON
--------------------------------------------------------------------------------
Twenty consecutive thought bubbles from one character on one page are ONE slot,
not twenty. A slot ends when the speaker changes, or when the page ends. A run
that spills onto the next page becomes two slots, one per page, because
one-shots are page-scoped and reset on a page turn.

The author marks WHO SPEAKS AND IN WHAT ORDER. Nothing else. A page with 30
balloons is often 5 or 6 slots.

Consequences, all simplifications:
- rects is OPTIONAL. The VA sees the page and knows they are Aiko on run 3.
  The author draws boxes only to disambiguate a genuinely confusing page.
- "linked balloons" (one sentence split across two balloons) is not an edge
  case at all. It was never two things.
- unison (one balloon, several tails) is NOT a multi-cast slot. It is two
  slots chained at offset 0. No array column needed.
- authoring effort drops enormously, which is the real reason this is right:
  the owner has to mark every page of every comic by hand.

--------------------------------------------------------------------------------
15.2 CHAINS ABSORB EVERY OVERLAP CASE
--------------------------------------------------------------------------------
Section 13 gave SFX an attach_to_slot_id + offset_ms. GENERALIZE IT TO EVERY
SLOT. Any slot may hang off any other. The reader's manual cursor exposes only
CHAIN HEADS (slots with no parent); one keypress fires a head and everything
attached to it, scheduled against its real start time.

That single mechanism covers:
  interruption (B starts before A ends)   crosstalk / people talking over
  unison (offset 0)                       a bleep landing over a word
  laughter or a gasp under a line         SFX landing on a punch
  a music cue firing off a specific line  (see 15.5)

THE MODEL IS UNCHANGED. Still exactly two triggers: page-triggered and
user-triggered. A chain is ONE user trigger.

PLAYBACK: NO POLYPHONY. Overlapping dialogue is PRERENDERED into one clip, so
the reader still plays a chain from a single element and the timing is baked in
rather than scheduled at runtime.

  THE CACHE KEY IS THE WHOLE POINT. Render PER CHAIN, keyed by a hash of the
  participating vo_clip ids, NOT per mix. Baking A+B together freezes that
  pairing, so if the key were anything coarser, swapping one character's VA
  would invalidate unrelated renders. Keyed this way, a swap re-renders only
  the chains that character appears in and everything else is a cache hit.
  Chains are short and most are a single clip, so the render space stays small.

  KEEP SOUNDTRACK CUES OUT OF THE RENDER. They play on their own element.
  Folding them in would multiply the render space by every VO x soundtrack
  pairing for no benefit.

  Four single-purpose elements total: audio.a / audio.b for music, one for the
  voice chain, one for soundtrack cues. No element pool, no scheduler.

--------------------------------------------------------------------------------
15.3 BALLOON STYLE IS NOT MODELLED
--------------------------------------------------------------------------------
Thought vs whisper vs shout vs radio vs telepathy: NO style enum, no per-slot
picker. Rejected as busywork for the author and noise in the schema.
Instead:
  - a short standing reminder in the VO booth that balloon shape carries
    delivery, and the art already says which is which
  - pages.note: optional author direction for a whole page ("this scene is
    a phone call", "she is barely conscious here")
  - LATER, optional: built-in reader effects (a radio filter and so on). Store
    nothing for it now. A VA performing it is usually better than a filter.

--------------------------------------------------------------------------------
15.4 FILLER VOs (the timing-reference problem)
--------------------------------------------------------------------------------
A VA often plays several characters, and should be encouraged to record scratch
"filler" for the OTHER characters in a scene so later VAs have something to time
against. Without it, everyone performs into a vacuum and the scene never locks.

vos.filler = true means: hidden from reader mix selection, excluded from
rankings, but SURFACED IN THE BOOTH as a timing reference track. It does not
disturb the full-coverage rule (D3) because a filler VO is not competing.

A VA covering several characters simply submits several VOs. That already works.

--------------------------------------------------------------------------------
15.5 MUSICIANS GET CUES TOO
--------------------------------------------------------------------------------
Previously only the author could define SFX slots and only music could be
page-triggered. Both restrictions lift:
- soundtrack_tracks.trigger_slot_id: a music track can start when a specific
  LINE fires, not only when a page turns ("the theme swells when he says her
  name"). Still user-triggered, because the reader fires the line.
- soundtrack_cues: a musician can attach their OWN audio to any slot with an
  offset, without the author having pre-marked an SFX slot for it. The same
  table also fills author-defined SFX slots (slot_id set instead).

--------------------------------------------------------------------------------
15.6 THE REST OF THE CATALOGUE, AND WHERE EACH LANDS
--------------------------------------------------------------------------------
NARRATION        cast_members.kind='narrator'. Castable and dubbable, just
                 never has a balloon tail. First-person retrospective captions
                 are the character's own cast id.
BACKGROUND SOUND slots.trigger='page' on an sfx slot: it fires on page arrival.
                 This is page-triggered audio, the same class as music, NOT a
                 third ambient layer (which section 13 rejected and still does).
CROWD / WALLA    THE RULE: if a specific named character says it, it is VO. If
                 nobody does, it is SFX. Settles most attribution arguments.
LAUGHS, SCREAMS, GRUNTS, CRYING
                 VO, not SFX. It is that character's voice, and it counts
                 toward coverage, which is correct.
EFFORT NOISES with no balloon    sfx slot.
EXPLETIVES / GRAWLIX (@#$%!)
                 NOT solved in the mix. vos.explicit lets a VA deliver censored
                 or uncensored, and competing VOs already exist, so a clean dub
                 and a filthy dub are simply two VOs. Zero new machinery.
FOREIGN LANGUAGE Left entirely to the VA. They will read what is written.
                 No schema, no translation slots.
FLASHBACK / AGED VOICE / POSSESSION
                 A SEPARATE CAST MEMBER ("young aiko"). No schema change, and
                 it lets the author cast a different VA for it.
CHARACTER WHO NEVER SPEAKS       coverage counts SLOTS, not cast members, so a
                 silent character is simply skipped. Avoids a divide by zero.
MYSTERY SPEAKER  the author names the cast entry "???" . No schema.
SIGNAGE, ENVIRONMENTAL TEXT      not a slot.
BOLD / ITALIC EMPHASIS           nothing. The art carries it.
A PAGE WITH 20 RUNS              a reader-side auto-advance setting, which per
                 the section 3 rule must never touch the manifest.

--------------------------------------------------------------------------------
15.7 COMPOSER / BOOTH UI THIS IMPLIES
--------------------------------------------------------------------------------
1. Numbered reading-order overlay on the page, draggable to reorder. Order is
   the one thing only the author can get right and it is currently invisible.
2. Chain authoring: drag one slot onto another to attach it, plus an offset
   scrubber. This single control IS the SFX-attach UI, the crosstalk UI, the
   interruption UI and the music-cue UI.
3. Optional rect drawing, not required to author a page.
4. pages.note field.
5. Coverage meter counts line slots for one character, excluding sfx.
6. Booth: previous slot plays before recording, filler VOs available as
   reference, drag-and-drop import of externally recorded audio (D-list).

--------------------------------------------------------------------------------
15.8 OPEN / BUGS
--------------------------------------------------------------------------------
SINGING - INTRACTABLE, ACCEPTED AS SUCH. A sung line has to sit in the
  soundtrack's key and tempo, but the VA and the composer are independent
  contributors. Not solving it. Do not spend design effort here; if a sung
  comic works it will be because one canonical mix was coordinated by hand.
BUG - SPREAD MODE HIDES HALF THE DIALOGUE. refreshOneshots() builds os.list
  from audioPage(), and audioPage() returns only the leftmost visible page
  (rightmost in RTL). In a two-page spread the other page's lines cannot be
  triggered AT ALL.
  NOTE: PREFETCHING DOES NOT FIX THIS. It is a reachability bug, not a loading
  one: those slots have no entry in the cursor, so no amount of preloaded audio
  makes them playable. The fix is merging one-shots from BOTH visible pages
  into os.list in reading order. Prefetching the next page's audio is still
  worth doing, just as a separate performance win.

================================================================================
16. SCREEN PLAN  (every surface the platform needs; 2026-07-25)
================================================================================
Today there are four surfaces: index.html (library + home), read.html (reader),
studio.html (authoring), spec.html. Ten more are needed. Nothing here is
built. Order of construction is in 14.9, not here.

--------------------------------------------------------------------------------
16.1 READER SURFACES (three, all inside read.html)
--------------------------------------------------------------------------------
EVERY ONE OF THESE IS HIDDEN IN FOCUS/FULLSCREEN MODE. body.focus already
hides the header and footer; these join that rule. Focus mode is the comic and
nothing else.

R-A  CAST BAR (before you start reading)
     Shown on arrival, dismissible, not a wall. Contains:
       - each character: name, the VA cast by default (the most popular VO),
         completion %, a play button for that VO's preview clip, and a link to
         the VA's profile
       - the soundtrack: composer name, preview, link to profile
       - the comic's creator(s), linked
       - the description
       - what is next in the series
     "Most popular" is the default cast. The author's canonical mix wins when
     nothing has plays yet (see D5).

R-B  SWAP PANEL (during reading, any time)
     Hot-swap ANY character's VA, or the whole soundtrack, mid-read. Rows sorted
     by popularity, each showing completion % and a preview. Applying re-resolves
     the current page and keeps the reader where it is. Loading is expected to
     take a moment; show it, do not block the page.

R-C  COMMENT RAIL (click to reveal)
     Collapsed by default. Clicking PUSHES THE PAGE ASIDE rather than overlaying
     it, so comments read without scrolling the comic away, and the layout stays
     balanced. Per-page threads.
     CLICK-TO-REVEAL IS THE SPOILER GUARD. Comments are never visible until
     asked for, so a reader on page 4 cannot be spoiled by a page 40 thread.

--------------------------------------------------------------------------------
16.2 NEW PAGES (seven)
--------------------------------------------------------------------------------
P-1  COMIC PAGE. Replaces today's small cover modal. Cover, title, description,
     creator(s), the full cast table (character / VA / completion / preview),
     available soundtracks, kudos, comments, series navigation, and the read CTA.
     This is the page a shared link should point at, not the reader.
P-2  CONTRIBUTOR PROFILE. A VA's or composer's VOs and soundtracks with
     previews, the comics they have worked on, play and kudos counts, follow.
P-3  CONTRIBUTE HUB. Comics open for voice or music, with OPEN ROLES surfaced
     (see 16.3). The page that turns a lurker into a contributor.
P-4  SERIES PAGE. Chapters in order, with per-chapter cast availability.
P-5  CREATOR DASHBOARD (author only). Cast management, slot marking entry point,
     consent settings per axis, canonical mix selection, reports on their work.
P-6  AUTH. Sign in, and a "check your email" screen. Magic link only (14.8).
P-7  MOD QUEUE (owner only). Reports table with a takedown action. The owner is
     the sole moderator (D5), so a table is enough. No UI polish.

PLUS A FLOW, NOT A SCREEN: publish from the authoring app. Export, upload,
attach to a comic as a VO or a soundtrack. Lands with step 11.

--------------------------------------------------------------------------------
16.3 SOCIAL FEATURES  (ACCEPTED 2026-07-25, build them)
--------------------------------------------------------------------------------
All four below are decided, not suggestions. They exist to drive collaboration,
not engagement metrics.
REJECTED: the credits roll at the end of a comic. Also dropping meta.credits
from step 10, since a display-only credits field with no surface to display it
on is dead weight; real attribution comes from mixes.
KUDOS. AO3 model: one per account per VO or soundtrack, no removal, no
  numeric gaming surface beyond the count. Cheapest possible way to let a
  reader thank a contributor.
CASTING CALLS / OPEN ROLES. THE HIGHEST-VALUE ONE, and nearly free: a comic
  with open voice consent and an uncast character ALREADY IS an open role. It
  is a query, not a feature. Surfacing that list is what makes P-3 worth
  visiting and is the main lever against the cold-start problem in D-note 5.
FOLLOW A CONTRIBUTOR, notify on publish. How a VA builds an audience that
  comes back, rather than one that happened to click once.
FILLER BADGE on comics that carry reference tracks (15.4). Directly lowers the
  barrier for the next VA, which is the entire point of filler VOs.

DEFERRED, deliberately: troupes/groups, bookmarks and reading lists, direct
messages. All reasonable later. None of them unblock anything now.

--------------------------------------------------------------------------------
16.4 NOTES
--------------------------------------------------------------------------------
- Comments need moderation and the owner is the only moderator. Ship them with
  a report action wired to the same reports table as VOs and soundtracks.
- P-1 becomes the canonical shareable URL. OG/meta tags belong there (backlog).
- R-A and P-1 show nearly the same information. Build P-1 first and let R-A be
  a condensed version of it, not a second implementation.

--------------------------------------------------------------------------------
DONE THIS PROJECT (so far), for context:
split reader/composer/library into separate pages; layered UI + Lucide icons;
colour-match tint toggle + neutral dark theme; real fullscreen; lazy page/audio
loading; queues + silence + playlist; jump/spread audio correctness; deep links;
?read=; RTL; page zoom/pan; big-page layout fix (min-height:0 on .viewer-col);
fullscreen settings fix; composer rebuild (media bay, video-editor timeline,
slip/grip, minimap, waveforms, clipbar, trash bar, undo/redo, folder upload,
dock search/sort/filter, loading overlay); CBZ/PDF import; export packs as-is
(opus removed); library folder auto-populate + cover extraction + read-it modal;
one-shots end-to-end (manifest type, composer block+queue panel, reader down/up +
soundtrack pause on space + one-shot pause on shift+space + loop setting + corner
indicator with playing state + tutorial + hotkeys panel); service worker made
network-first for HTML; ~135-assertion Playwright smoke suite, zero console errors.
================================================================================
